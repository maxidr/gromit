const test = require('tape')
const errors = require('../lib/errors')

test('errors', function(t){
  /*
  t.equals(errors(), null)

  errors('name', 'name is required')
  t.same(errors('name'), ['name is required']);

  errors('name', 'name should be John');
  t.same(errors('name'), ['name is required', 'name should be John'])

  errors('lastName', 'last name must be Doe')
  t.same(errors('lastName'), ['last name must be Doe'])

  t.same(errors(), {
    name: ['name is required', 'name should be John'],
    lastName: ['last name must be Doe']
  })

  errors.clear();
  t.equals(errors(), null)

  t.end();
  */
})
